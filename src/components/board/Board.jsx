import React, { useState } from 'react'
import {OPERATORS} from '../../constants/constants'

function Board() {

  // const [board, setBoard] = useState<OPERATORS[row][col]>([]);
  
  return (
    <div>

    </div>
  )
}

export default Board